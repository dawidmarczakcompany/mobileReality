import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderColor: 'rgba(1,1,1,0.2)',
    borderWidth: 1,
    borderRadius: 8,
    height: 40,
    padding: 8,
    marginBottom: 16,
  },
  errorMessage: {
    color: 'red',
    marginBottom: 4,
  },
});
